spring:
  datasource:
    url: jdbc:h2:mem:permissio_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false
